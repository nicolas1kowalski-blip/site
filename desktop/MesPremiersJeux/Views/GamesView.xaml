<UserControl x:Class="MesPremiersJeux.Views.GamesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="#FFF3E9FF">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" Padding="12,14">
            <StackPanel x:Name="Picker" />
        </ScrollViewer>

        <Grid Grid.Column="1" x:Name="GameHost" />

        <Canvas Grid.ColumnSpan="2" x:Name="Confetti" IsHitTestVisible="False" ClipToBounds="True" />
    </Grid>
</UserControl>
